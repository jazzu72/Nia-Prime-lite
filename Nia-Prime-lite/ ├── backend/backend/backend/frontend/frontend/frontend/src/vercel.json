{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }],
  "routes": [
    { "src": "/chat", "dest": "https://YOUR-RENDER-BACKEND.onrender.com/chat" }
  ]
}
